# ─── Required ────────────────────────────────────────────────
GROQ_API_KEY="your_groq_api_key_here"

# ─── Transcription ───────────────────────────────────────────
AI_ENHANCE="off"                       # "on" or "off"
AI_PROMPT_STYLE="clarify"             # strict | professional | minimal | formal_arabic | assistant | clarify
TRANSCRIPTION_LANG="en"                # Language code (e.g., "en", "ar", "es") or empty for auto-detection
AI_MODEL="auto"                        # "auto" for smart fallback, or a specific model ID

# ─── Optional Overrides ─────────────────────────────────────
# These have smart defaults and usually don't need to be set.
# SOUND_FILE="/custom/path/to/sound.oga"       # Default: <project>/assets/Staplebops.oga
# HISTORY_FILE="/custom/path/to/history.md"     # Default: <project>/history.md
# ENABLE_OVERLAY="auto"                         # "auto" | "on" | "off" (auto = Linux only)
# AUDIO_INPUT_DEVICE="0"                        # macOS only: ffmpeg avfoundation input index
